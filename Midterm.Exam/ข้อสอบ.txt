ข้อสอบมีดังนี้
1. ให้เขียน function คำนวนพื้นที่วงกลม โดยรับค่า รัศมีเข้ามาแล้วคำนวณ
hint: การรับค่าใช้ function input() เช่น
radius = input("input the circle radius")
2. ให้เขียน function คำนวนพื้นที่สามเหลี่ยม โดยครับค่า ฐาน กับ สูง
3. ให้ทำ menu ให้ผู้ใช้เลือกใช้ function หน้าพื้นที่ได้ตามต้องการ
4. เสร็จแล้วให้ file ของ นศ. ขึ้น git hub โดยกำหนดชื่อ file เป็นรหัส นศ.